Create a "Hello, World!" application in Go, using `charmbracelet/bubbletea` to make it fun. Get creative — animation, color, interactivity, whatever you like.

The only strict requirement: pressing `q` must always quit the program.

Provide the complete source code and anything else needed to build and run it.
